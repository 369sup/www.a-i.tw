# Bounded contexts

狀態：Current / pending owner

目前尚未宣告正式 Bounded Context owner。規劃中的 strategic map 請參考 [`context-map.md`](context-map.md)；它不是 runtime proof。

每個 context 必須記錄 responsibility、owner、in/out scope、aggregates、use cases、ports、contracts、upstream/downstream 與 boundary debt。
