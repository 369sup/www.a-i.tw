# Decisions

狀態：Current

此目錄是正式 Architecture Decision Record（ADR）的唯一位置。新增或拆分 Bounded Context、改變 owner、Aggregate、Context Map、依賴方向或公開 contract 版本策略時，必須建立 ADR。

ADR 的四位數編號、檔名前綴與標題編號必須一致且在本目錄唯一；superseded ADR 保留原編號，不得重用。
