# Dependency rules governance index

狀態：Current

The canonical technical dependency rules are [`../architecture/dependency-rules.md`](../architecture/dependency-rules.md). This governance index owns review requirements: boundary changes require Context Map review; public contract changes require compatibility review; new Shared Kernel, module, owner or data boundary requires an ADR.
