# 語意驅動開發生命週期

依 `docs/engineering/semantic-development-workflow.md` 執行 G0-G7。先完成 Serena
handshake 與 repository orientation；再核准語意、更新 canonical docs、確認 owner／path，
之後才能 scaffold 與 inside-out implementation。完成 composition／inbound adapters 後執行
diagnostics、check、docs、architecture、build、Semgrep 與 acceptance verification。逐 gate
回報 evidence，不得跳過或用 UI／資料表反推 Domain。
