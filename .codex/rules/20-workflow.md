# Workflow rules

- Inspect worktree and local instructions before editing.
- Prefer `rg`, `find`, and focused validation over broad rewrites.
- Never commit secrets, credentials, personal data, or machine-specific paths.
- Keep docs-only changes free of runtime drift.
- Use Turbo for task orchestration when multiple packages are introduced.
