# E2E tests

Place product-flow specifications under a bounded-context folder, for example
`authentication-security/sign-in.spec.ts`. Add a flow only after its acceptance criteria
and runtime owner are defined.
