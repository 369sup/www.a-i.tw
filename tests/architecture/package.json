{
  "name": "@a-i/architecture-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest --configLoader runner run",
    "check": "pnpm lint && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@a-i/eslint-config": "workspace:*",
    "@a-i/typescript-config": "workspace:*",
    "@types/node": "^20",
    "dependency-cruiser": "18.1.0",
    "typescript": "^5",
    "vitest": "4.1.10"
  }
}
