# Repository agent assets

Repository-owned reusable workflows are distributed through the marketplace in
`.agents/plugins/marketplace.json`. Plugin source lives under `plugins/`.

`.agents/skills/` is reserved for exceptional standalone repository skills and currently contains only its routing index.
