---
name: repo-explore-first
description: Inspect project rules and architecture before changing this repository.
---

Read `AGENTS.md` and `.codex/AGENTS.md`, then use the root `Minimal-context routing` table. Do not preload
`docs/README.md`, `docs/ai-index.md`, or the architecture catalog when the task already names a narrow runtime owner.
Read `docs/ai-index.md` for docs-only, product-semantics, or ambiguous ownership tasks; read `docs/README.md` only
for documentation-topology changes; read the architecture catalog only for canonical-concern governance.

Inspect worktree status and the narrowest relevant manifest, source and tests. Report ownership, dependency direction,
use case, Ports, Adapters and composition-root impact in one compact note before editing. Stop exploring once those
facts and required verification are known. Treat runtime evidence and Context Map manifests as authoritative over
copied skills or navigation memories.
