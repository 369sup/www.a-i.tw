# {{subdomain}}

Internal {{type}} subdomain of {{context}}. It is not a separate Bounded Context.

This template is materialized only after the owning Context and internal subdomain
have been approved. It deliberately has no `context.json`, package exports, or
cross-context contract.
