# __DOMAIN__ Context rules

Owner: __OWNER__. Organize each layer by declared subdomain. External consumers use only `public-api.ts` or approved
contracts. Ownership-free shared directories are forbidden.
