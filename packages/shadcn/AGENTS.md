# UI package rules

- This package owns reusable shadcn primitives and presentation utilities only.
- Do not put domain rules, data fetching, route handlers, or product-specific use cases here.
- Preserve keyboard access, focus states, semantic HTML, and the existing Tailwind token system.
