# Domain

Owns Repository name, visibility/lifecycle transitions, Principal/Team grant subjects and the centralized role/action
decision policy. It never imports Account facts directly and has no framework or external-package dependencies.
