# Sub Template supporting subdomain

This is an internal supporting subdomain of `master-template`, not a second
bounded context. Its Domain/Application/Infrastructure layers stay within the
owner package and are composed only by the Master Template composition factory.
