# access-policy

This directory is a Domain Area navigation and governance boundary. It owns no runtime, Aggregate, Contract, table, transaction, composition, or dependency. Runtime code belongs only to child Bounded Contexts.
