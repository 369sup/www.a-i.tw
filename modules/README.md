# Bounded Context workspaces

This folder contains real product bounded contexts. It intentionally contains no
example context: the current strategic map has no approved runtime owner or
subdomain yet. Create one with `pnpm generate:context` only after its owner,
subdomain, public language, and Context Map relationship are defined.
